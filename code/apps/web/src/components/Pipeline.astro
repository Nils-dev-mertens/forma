<section id="pipeline" class="h-[426px] px-[clamp(20px,4vw,48px)] flex flex-col items-center justify-center max-lg:h-auto max-lg:py-[60px] max-sm:py-12">
  <div class="container">
    <h2 class="font-black text-[44px] max-sm:text-[32px]">THE PIPELINE</h2>
    <div class="w-full h-1 bg-ink my-4 mb-12 max-sm:mb-8"></div>
    <div class="flex gap-6 w-full items-stretch max-lg:flex-col">
      <div class="flex-1 bg-card rounded p-6 flex flex-col gap-3 max-sm:p-5">
        <span class="font-black text-[56px] text-accent leading-none max-sm:text-[44px]">001</span>
        <span class="font-mono text-[15px] font-bold">DEFINE IDENTITY</span>
        <p class="font-body text-sm leading-[1.5] text-ink">Fill one record: name, title, links, brand colors. Your single source of truth.</p>
      </div>
      <span class="font-black text-[34px] flex items-center shrink-0 max-lg:rotate-90 max-lg:self-center">→</span>
      <div class="flex-1 bg-card rounded p-6 flex flex-col gap-3 max-sm:p-5">
        <span class="font-black text-[56px] text-accent leading-none max-sm:text-[44px]">002</span>
        <span class="font-mono text-[15px] font-bold">BIND A TEMPLATE</span>
        <p class="font-body text-sm leading-[1.5] text-ink">Pick a built-in HTML template or write your own. Slots pull straight from the record.</p>
      </div>
      <span class="font-black text-[34px] flex items-center shrink-0 max-lg:rotate-90 max-lg:self-center">→</span>
      <div class="flex-1 bg-card rounded p-6 flex flex-col gap-3 max-sm:p-5">
        <span class="font-black text-[56px] text-accent leading-none max-sm:text-[44px]">003</span>
        <span class="font-mono text-[15px] font-bold">RENDER + EXPORT</span>
        <p class="font-body text-sm leading-[1.5] text-ink">One click renders every asset to PNG. Data changed? Re-render everything at once.</p>
      </div>
    </div>
  </div>
</section>
